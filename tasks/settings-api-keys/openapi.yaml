openapi: 3.0.3
info:
  title: Developer API Keys Settings Page UI Fixture API
  version: 1.0.0
paths:
  /api/settings-api-keys:
    get:
      summary: Fetch data for Developer API Keys Settings Page UI
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

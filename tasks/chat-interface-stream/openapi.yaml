openapi: 3.0.3
info:
  title: Live Streaming Chat Interface UI Fixture API
  version: 1.0.0
paths:
  /api/chat-interface-stream:
    get:
      summary: Fetch data for Live Streaming Chat Interface UI
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

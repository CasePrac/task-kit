openapi: 3.0.3
info:
  title: Bank Transfer Flow Fixture API
  version: 1.0.0
paths:
  /api/fintech-transfer-flow:
    get:
      summary: Fetch data for Bank Transfer Flow
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

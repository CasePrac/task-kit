openapi: 3.0.3
info:
  title: Sales & Metrics Analytics Dashboard Fixture API
  version: 1.0.0
paths:
  /api/analytics-dashboard:
    get:
      summary: Fetch data for Sales & Metrics Analytics Dashboard
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

openapi: 3.0.3
info:
  title: Interactive Kanban Task Board Fixture API
  version: 1.0.0
paths:
  /api/kanban-board:
    get:
      summary: Fetch data for Interactive Kanban Task Board
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

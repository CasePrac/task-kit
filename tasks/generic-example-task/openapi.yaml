openapi: 3.0.3
info:
  title: Generic Example Task Fixture API
  version: 1.0.0
paths:
  /api/generic-example-task:
    get:
      summary: Fetch data for Generic Example Task
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    example: item-1
                  title:
                    type: string
                    example: Generic Item
